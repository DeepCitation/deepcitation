{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- .",
  "github": {
    "enabled": true,
    "silent": true
  },
  "buildCommand": "(cd ../../ && npm install --include=dev && npm run build) && npm run build"
}
