{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- .",
  "buildCommand": "(cd ../../ && npm install && npm run build) && npm run build",
  "github": {
    "enabled": true,
    "silent": true
  }
}
